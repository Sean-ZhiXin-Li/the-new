# Phase 0 Day 1 — AI Foundation

Model:
y = f(x; theta)

In this task:
prediction = weight * x + bias

Definitions:
- x = input
- theta = parameters
- prediction = model output
- target = desired output
- loss = squared prediction error

Key Lesson:
AI begins as function approximation and parameter optimization.